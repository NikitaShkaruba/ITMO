#include <xil_io.h>
#define GPIO_ADDRESS  0x40000000
#define TIMER_ADDRESS  0x41C00000

//typedef unsigned int u32;

int main() {
	u32 per=50;
	Xil_Out32(GPIO_ADDRESS, per);
	Xil_Out32(TIMER_ADDRESS, per);
	while(1);
}
